type init-btmac-sh, domain;
type init-btmac-sh_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(init-btmac-sh)

allow init-btmac-sh vendor_toolbox_exec:file rx_file_perms;

# Set the persist.vendor.service.bdroid.bdaddr property
set_prop(init-btmac-sh, vendor_bluetooth_prop)
